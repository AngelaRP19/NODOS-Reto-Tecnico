spring:
  application:
    name: reto-tecnico

  datasource:
    url: jdbc:postgresql://localhost:5432/nodos_db
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: reto

  sql:
    init:
      mode: always

server:
  port: 8081